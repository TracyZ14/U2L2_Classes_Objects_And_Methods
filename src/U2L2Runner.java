public class U2L2Runner {
    public static void U2L2(String[] args) {
        // Code segment I
        // int len = 65;
        // int wid = len + 10;
        // Rectangle rect = new Rectangle(len, wid);

        // Code segment II
        // Rectangle rect = new Rectangle(10.0, 30.0);

        // Code segment III
        // Rectangle rect = new Rectangle(0);
    }
}
